services:
  - type: web
    name: your-service-name
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "/opt/render/project/src/.venv/bin/gunicorn monprojet.wsgi --bind 0.0.0.0:$PORT"